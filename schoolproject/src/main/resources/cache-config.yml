spring:
  cache:
    type: redis
    redis:
      time-to-live: 600000 # 缓存过期时间，单位毫秒
      cache-null-values: false # 是否缓存空值
      use-key-prefix: true # 是否使用key前缀
      key-prefix: "schoolproject:" # key前缀